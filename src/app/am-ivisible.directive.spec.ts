import { AmIVisibleDirective } from './am-ivisible.directive';

describe('AmIVisibleDirective', () => {
  it('should create an instance', () => {
    const directive = new AmIVisibleDirective();
    expect(directive).toBeTruthy();
  });
});
